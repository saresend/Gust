

/// The FileType enum is used to keep track of the different file formats Gust can output to, as well 
/// specifying the type of file you want to output to, when actually building a visualization
pub enum FileType {
    JSON,
    HTML,
}
