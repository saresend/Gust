
use axes::{XAxis, YAxis};
use bar::Bar;

pub struct BarChart {
    color : (u8, u8, u8), //Denotes the color as RGB
    x_axis : XAxis,
    y_axis: YAxis,
    show_grid: bool,
    bars: Vec<Bar>,
}



impl BarChart {
    


}
